1-#This tells Git: “track this folder
git init   


2-#stages all files
git add .
3 # save the changes
git commit -m "Initial commit"

4- link your repo
git remote add origin https://github.com/rajonkar/Myhobby.git


5-#Push your code
git branch -M main

6-#First push uses -u to link your branch
git push -u origin main

# see if any untracked file
# git status

#---------------------------------#

#git add . → stages all files

#git commit → saves a version locally

#Copy the repo URL (looks like: https://github.com/username/repo.git)


git remote add origin https://github.com/username/repo.git

# Question: Every time i make  changes to exsitng file i need to excute git add
Answer below

You might change 5 files but want to commit only 2

git add file1.py file2.py
git commit -m "fixed bug"


#---- how to clone from one repo to another--------

step 1
git clone https://github.com/LinkedInLearning/python-ET-4314028	Clone the source repo locally. (You should be in the folder)

(you make sure that you see git folder using ls-a) 
If not git command wont work

step 2: #Rename the existing remote to upstream
git remote rename origin upstream	

step 3: Asdd your repo
git remote add origin https://github.com/rajonkar/MyLearning.git	

step4: Create your branch..here the metatdata in the git is updated
git checkout -b python_linkedin_learning

step5: Push the changes to your branch on repo
git push -u origin python_linkedin_learning	
